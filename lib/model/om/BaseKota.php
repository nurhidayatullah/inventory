<?php


/**
 * Base class that represents a row from the 'kota' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.7.0 on:
 *
 * 07/21/15 02:00:10
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseKota extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'KotaPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        KotaPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the nama_kota field.
     * @var        string
     */
    protected $nama_kota;

    /**
     * The value for the id_propinsi field.
     * @var        int
     */
    protected $id_propinsi;

    /**
     * @var        Propinsi
     */
    protected $aPropinsi;

    /**
     * @var        PropelObjectCollection|Customers[] Collection to store aggregation of Customers objects.
     */
    protected $collCustomerss;
    protected $collCustomerssPartial;

    /**
     * @var        PropelObjectCollection|Produsen[] Collection to store aggregation of Produsen objects.
     */
    protected $collProdusens;
    protected $collProdusensPartial;

    /**
     * @var        PropelObjectCollection|Supplier[] Collection to store aggregation of Supplier objects.
     */
    protected $collSuppliers;
    protected $collSuppliersPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $customerssScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $produsensScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $suppliersScheduledForDeletion = null;

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {

        return $this->id;
    }

    /**
     * Get the [nama_kota] column value.
     *
     * @return string
     */
    public function getNamaKota()
    {

        return $this->nama_kota;
    }

    /**
     * Get the [id_propinsi] column value.
     *
     * @return int
     */
    public function getIdPropinsi()
    {

        return $this->id_propinsi;
    }

    /**
     * Set the value of [id] column.
     *
     * @param  int $v new value
     * @return Kota The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = KotaPeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [nama_kota] column.
     *
     * @param  string $v new value
     * @return Kota The current object (for fluent API support)
     */
    public function setNamaKota($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (string) $v;
        }

        if ($this->nama_kota !== $v) {
            $this->nama_kota = $v;
            $this->modifiedColumns[] = KotaPeer::NAMA_KOTA;
        }


        return $this;
    } // setNamaKota()

    /**
     * Set the value of [id_propinsi] column.
     *
     * @param  int $v new value
     * @return Kota The current object (for fluent API support)
     */
    public function setIdPropinsi($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->id_propinsi !== $v) {
            $this->id_propinsi = $v;
            $this->modifiedColumns[] = KotaPeer::ID_PROPINSI;
        }

        if ($this->aPropinsi !== null && $this->aPropinsi->getId() !== $v) {
            $this->aPropinsi = null;
        }


        return $this;
    } // setIdPropinsi()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->nama_kota = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->id_propinsi = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 3; // 3 = KotaPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Kota object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aPropinsi !== null && $this->id_propinsi !== $this->aPropinsi->getId()) {
            $this->aPropinsi = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(KotaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = KotaPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aPropinsi = null;
            $this->collCustomerss = null;

            $this->collProdusens = null;

            $this->collSuppliers = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(KotaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = KotaQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseKota:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseKota:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(KotaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseKota:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseKota:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                KotaPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aPropinsi !== null) {
                if ($this->aPropinsi->isModified() || $this->aPropinsi->isNew()) {
                    $affectedRows += $this->aPropinsi->save($con);
                }
                $this->setPropinsi($this->aPropinsi);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->customerssScheduledForDeletion !== null) {
                if (!$this->customerssScheduledForDeletion->isEmpty()) {
                    CustomersQuery::create()
                        ->filterByPrimaryKeys($this->customerssScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->customerssScheduledForDeletion = null;
                }
            }

            if ($this->collCustomerss !== null) {
                foreach ($this->collCustomerss as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->produsensScheduledForDeletion !== null) {
                if (!$this->produsensScheduledForDeletion->isEmpty()) {
                    ProdusenQuery::create()
                        ->filterByPrimaryKeys($this->produsensScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->produsensScheduledForDeletion = null;
                }
            }

            if ($this->collProdusens !== null) {
                foreach ($this->collProdusens as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->suppliersScheduledForDeletion !== null) {
                if (!$this->suppliersScheduledForDeletion->isEmpty()) {
                    SupplierQuery::create()
                        ->filterByPrimaryKeys($this->suppliersScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->suppliersScheduledForDeletion = null;
                }
            }

            if ($this->collSuppliers !== null) {
                foreach ($this->collSuppliers as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = KotaPeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . KotaPeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(KotaPeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`id`';
        }
        if ($this->isColumnModified(KotaPeer::NAMA_KOTA)) {
            $modifiedColumns[':p' . $index++]  = '`nama_kota`';
        }
        if ($this->isColumnModified(KotaPeer::ID_PROPINSI)) {
            $modifiedColumns[':p' . $index++]  = '`id_propinsi`';
        }

        $sql = sprintf(
            'INSERT INTO `kota` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`id`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`nama_kota`':
                        $stmt->bindValue($identifier, $this->nama_kota, PDO::PARAM_STR);
                        break;
                    case '`id_propinsi`':
                        $stmt->bindValue($identifier, $this->id_propinsi, PDO::PARAM_INT);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aPropinsi !== null) {
                if (!$this->aPropinsi->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aPropinsi->getValidationFailures());
                }
            }


            if (($retval = KotaPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collCustomerss !== null) {
                    foreach ($this->collCustomerss as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collProdusens !== null) {
                    foreach ($this->collProdusens as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collSuppliers !== null) {
                    foreach ($this->collSuppliers as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = KotaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getNamaKota();
                break;
            case 2:
                return $this->getIdPropinsi();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Kota'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Kota'][$this->getPrimaryKey()] = true;
        $keys = KotaPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getNamaKota(),
            $keys[2] => $this->getIdPropinsi(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }

        if ($includeForeignObjects) {
            if (null !== $this->aPropinsi) {
                $result['Propinsi'] = $this->aPropinsi->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collCustomerss) {
                $result['Customerss'] = $this->collCustomerss->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collProdusens) {
                $result['Produsens'] = $this->collProdusens->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collSuppliers) {
                $result['Suppliers'] = $this->collSuppliers->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = KotaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setNamaKota($value);
                break;
            case 2:
                $this->setIdPropinsi($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = KotaPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setNamaKota($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setIdPropinsi($arr[$keys[2]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(KotaPeer::DATABASE_NAME);

        if ($this->isColumnModified(KotaPeer::ID)) $criteria->add(KotaPeer::ID, $this->id);
        if ($this->isColumnModified(KotaPeer::NAMA_KOTA)) $criteria->add(KotaPeer::NAMA_KOTA, $this->nama_kota);
        if ($this->isColumnModified(KotaPeer::ID_PROPINSI)) $criteria->add(KotaPeer::ID_PROPINSI, $this->id_propinsi);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(KotaPeer::DATABASE_NAME);
        $criteria->add(KotaPeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Kota (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setNamaKota($this->getNamaKota());
        $copyObj->setIdPropinsi($this->getIdPropinsi());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getCustomerss() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addCustomers($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getProdusens() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addProdusen($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getSuppliers() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addSupplier($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Kota Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return KotaPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new KotaPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Propinsi object.
     *
     * @param                  Propinsi $v
     * @return Kota The current object (for fluent API support)
     * @throws PropelException
     */
    public function setPropinsi(Propinsi $v = null)
    {
        if ($v === null) {
            $this->setIdPropinsi(NULL);
        } else {
            $this->setIdPropinsi($v->getId());
        }

        $this->aPropinsi = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Propinsi object, it will not be re-added.
        if ($v !== null) {
            $v->addKota($this);
        }


        return $this;
    }


    /**
     * Get the associated Propinsi object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Propinsi The associated Propinsi object.
     * @throws PropelException
     */
    public function getPropinsi(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aPropinsi === null && ($this->id_propinsi !== null) && $doQuery) {
            $this->aPropinsi = PropinsiQuery::create()->findPk($this->id_propinsi, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aPropinsi->addKotas($this);
             */
        }

        return $this->aPropinsi;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('Customers' == $relationName) {
            $this->initCustomerss();
        }
        if ('Produsen' == $relationName) {
            $this->initProdusens();
        }
        if ('Supplier' == $relationName) {
            $this->initSuppliers();
        }
    }

    /**
     * Clears out the collCustomerss collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Kota The current object (for fluent API support)
     * @see        addCustomerss()
     */
    public function clearCustomerss()
    {
        $this->collCustomerss = null; // important to set this to null since that means it is uninitialized
        $this->collCustomerssPartial = null;

        return $this;
    }

    /**
     * reset is the collCustomerss collection loaded partially
     *
     * @return void
     */
    public function resetPartialCustomerss($v = true)
    {
        $this->collCustomerssPartial = $v;
    }

    /**
     * Initializes the collCustomerss collection.
     *
     * By default this just sets the collCustomerss collection to an empty array (like clearcollCustomerss());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initCustomerss($overrideExisting = true)
    {
        if (null !== $this->collCustomerss && !$overrideExisting) {
            return;
        }
        $this->collCustomerss = new PropelObjectCollection();
        $this->collCustomerss->setModel('Customers');
    }

    /**
     * Gets an array of Customers objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Kota is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Customers[] List of Customers objects
     * @throws PropelException
     */
    public function getCustomerss($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collCustomerssPartial && !$this->isNew();
        if (null === $this->collCustomerss || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collCustomerss) {
                // return empty collection
                $this->initCustomerss();
            } else {
                $collCustomerss = CustomersQuery::create(null, $criteria)
                    ->filterByKota($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collCustomerssPartial && count($collCustomerss)) {
                      $this->initCustomerss(false);

                      foreach ($collCustomerss as $obj) {
                        if (false == $this->collCustomerss->contains($obj)) {
                          $this->collCustomerss->append($obj);
                        }
                      }

                      $this->collCustomerssPartial = true;
                    }

                    $collCustomerss->getInternalIterator()->rewind();

                    return $collCustomerss;
                }

                if ($partial && $this->collCustomerss) {
                    foreach ($this->collCustomerss as $obj) {
                        if ($obj->isNew()) {
                            $collCustomerss[] = $obj;
                        }
                    }
                }

                $this->collCustomerss = $collCustomerss;
                $this->collCustomerssPartial = false;
            }
        }

        return $this->collCustomerss;
    }

    /**
     * Sets a collection of Customers objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $customerss A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Kota The current object (for fluent API support)
     */
    public function setCustomerss(PropelCollection $customerss, PropelPDO $con = null)
    {
        $customerssToDelete = $this->getCustomerss(new Criteria(), $con)->diff($customerss);


        $this->customerssScheduledForDeletion = $customerssToDelete;

        foreach ($customerssToDelete as $customersRemoved) {
            $customersRemoved->setKota(null);
        }

        $this->collCustomerss = null;
        foreach ($customerss as $customers) {
            $this->addCustomers($customers);
        }

        $this->collCustomerss = $customerss;
        $this->collCustomerssPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Customers objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Customers objects.
     * @throws PropelException
     */
    public function countCustomerss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collCustomerssPartial && !$this->isNew();
        if (null === $this->collCustomerss || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collCustomerss) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getCustomerss());
            }
            $query = CustomersQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByKota($this)
                ->count($con);
        }

        return count($this->collCustomerss);
    }

    /**
     * Method called to associate a Customers object to this object
     * through the Customers foreign key attribute.
     *
     * @param    Customers $l Customers
     * @return Kota The current object (for fluent API support)
     */
    public function addCustomers(Customers $l)
    {
        if ($this->collCustomerss === null) {
            $this->initCustomerss();
            $this->collCustomerssPartial = true;
        }

        if (!in_array($l, $this->collCustomerss->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddCustomers($l);

            if ($this->customerssScheduledForDeletion and $this->customerssScheduledForDeletion->contains($l)) {
                $this->customerssScheduledForDeletion->remove($this->customerssScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Customers $customers The customers object to add.
     */
    protected function doAddCustomers($customers)
    {
        $this->collCustomerss[]= $customers;
        $customers->setKota($this);
    }

    /**
     * @param	Customers $customers The customers object to remove.
     * @return Kota The current object (for fluent API support)
     */
    public function removeCustomers($customers)
    {
        if ($this->getCustomerss()->contains($customers)) {
            $this->collCustomerss->remove($this->collCustomerss->search($customers));
            if (null === $this->customerssScheduledForDeletion) {
                $this->customerssScheduledForDeletion = clone $this->collCustomerss;
                $this->customerssScheduledForDeletion->clear();
            }
            $this->customerssScheduledForDeletion[]= $customers;
            $customers->setKota(null);
        }

        return $this;
    }

    /**
     * Clears out the collProdusens collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Kota The current object (for fluent API support)
     * @see        addProdusens()
     */
    public function clearProdusens()
    {
        $this->collProdusens = null; // important to set this to null since that means it is uninitialized
        $this->collProdusensPartial = null;

        return $this;
    }

    /**
     * reset is the collProdusens collection loaded partially
     *
     * @return void
     */
    public function resetPartialProdusens($v = true)
    {
        $this->collProdusensPartial = $v;
    }

    /**
     * Initializes the collProdusens collection.
     *
     * By default this just sets the collProdusens collection to an empty array (like clearcollProdusens());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initProdusens($overrideExisting = true)
    {
        if (null !== $this->collProdusens && !$overrideExisting) {
            return;
        }
        $this->collProdusens = new PropelObjectCollection();
        $this->collProdusens->setModel('Produsen');
    }

    /**
     * Gets an array of Produsen objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Kota is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Produsen[] List of Produsen objects
     * @throws PropelException
     */
    public function getProdusens($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collProdusensPartial && !$this->isNew();
        if (null === $this->collProdusens || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collProdusens) {
                // return empty collection
                $this->initProdusens();
            } else {
                $collProdusens = ProdusenQuery::create(null, $criteria)
                    ->filterByKota($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collProdusensPartial && count($collProdusens)) {
                      $this->initProdusens(false);

                      foreach ($collProdusens as $obj) {
                        if (false == $this->collProdusens->contains($obj)) {
                          $this->collProdusens->append($obj);
                        }
                      }

                      $this->collProdusensPartial = true;
                    }

                    $collProdusens->getInternalIterator()->rewind();

                    return $collProdusens;
                }

                if ($partial && $this->collProdusens) {
                    foreach ($this->collProdusens as $obj) {
                        if ($obj->isNew()) {
                            $collProdusens[] = $obj;
                        }
                    }
                }

                $this->collProdusens = $collProdusens;
                $this->collProdusensPartial = false;
            }
        }

        return $this->collProdusens;
    }

    /**
     * Sets a collection of Produsen objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $produsens A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Kota The current object (for fluent API support)
     */
    public function setProdusens(PropelCollection $produsens, PropelPDO $con = null)
    {
        $produsensToDelete = $this->getProdusens(new Criteria(), $con)->diff($produsens);


        $this->produsensScheduledForDeletion = $produsensToDelete;

        foreach ($produsensToDelete as $produsenRemoved) {
            $produsenRemoved->setKota(null);
        }

        $this->collProdusens = null;
        foreach ($produsens as $produsen) {
            $this->addProdusen($produsen);
        }

        $this->collProdusens = $produsens;
        $this->collProdusensPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Produsen objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Produsen objects.
     * @throws PropelException
     */
    public function countProdusens(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collProdusensPartial && !$this->isNew();
        if (null === $this->collProdusens || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collProdusens) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getProdusens());
            }
            $query = ProdusenQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByKota($this)
                ->count($con);
        }

        return count($this->collProdusens);
    }

    /**
     * Method called to associate a Produsen object to this object
     * through the Produsen foreign key attribute.
     *
     * @param    Produsen $l Produsen
     * @return Kota The current object (for fluent API support)
     */
    public function addProdusen(Produsen $l)
    {
        if ($this->collProdusens === null) {
            $this->initProdusens();
            $this->collProdusensPartial = true;
        }

        if (!in_array($l, $this->collProdusens->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddProdusen($l);

            if ($this->produsensScheduledForDeletion and $this->produsensScheduledForDeletion->contains($l)) {
                $this->produsensScheduledForDeletion->remove($this->produsensScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Produsen $produsen The produsen object to add.
     */
    protected function doAddProdusen($produsen)
    {
        $this->collProdusens[]= $produsen;
        $produsen->setKota($this);
    }

    /**
     * @param	Produsen $produsen The produsen object to remove.
     * @return Kota The current object (for fluent API support)
     */
    public function removeProdusen($produsen)
    {
        if ($this->getProdusens()->contains($produsen)) {
            $this->collProdusens->remove($this->collProdusens->search($produsen));
            if (null === $this->produsensScheduledForDeletion) {
                $this->produsensScheduledForDeletion = clone $this->collProdusens;
                $this->produsensScheduledForDeletion->clear();
            }
            $this->produsensScheduledForDeletion[]= $produsen;
            $produsen->setKota(null);
        }

        return $this;
    }

    /**
     * Clears out the collSuppliers collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Kota The current object (for fluent API support)
     * @see        addSuppliers()
     */
    public function clearSuppliers()
    {
        $this->collSuppliers = null; // important to set this to null since that means it is uninitialized
        $this->collSuppliersPartial = null;

        return $this;
    }

    /**
     * reset is the collSuppliers collection loaded partially
     *
     * @return void
     */
    public function resetPartialSuppliers($v = true)
    {
        $this->collSuppliersPartial = $v;
    }

    /**
     * Initializes the collSuppliers collection.
     *
     * By default this just sets the collSuppliers collection to an empty array (like clearcollSuppliers());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initSuppliers($overrideExisting = true)
    {
        if (null !== $this->collSuppliers && !$overrideExisting) {
            return;
        }
        $this->collSuppliers = new PropelObjectCollection();
        $this->collSuppliers->setModel('Supplier');
    }

    /**
     * Gets an array of Supplier objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Kota is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Supplier[] List of Supplier objects
     * @throws PropelException
     */
    public function getSuppliers($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collSuppliersPartial && !$this->isNew();
        if (null === $this->collSuppliers || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collSuppliers) {
                // return empty collection
                $this->initSuppliers();
            } else {
                $collSuppliers = SupplierQuery::create(null, $criteria)
                    ->filterByKota($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collSuppliersPartial && count($collSuppliers)) {
                      $this->initSuppliers(false);

                      foreach ($collSuppliers as $obj) {
                        if (false == $this->collSuppliers->contains($obj)) {
                          $this->collSuppliers->append($obj);
                        }
                      }

                      $this->collSuppliersPartial = true;
                    }

                    $collSuppliers->getInternalIterator()->rewind();

                    return $collSuppliers;
                }

                if ($partial && $this->collSuppliers) {
                    foreach ($this->collSuppliers as $obj) {
                        if ($obj->isNew()) {
                            $collSuppliers[] = $obj;
                        }
                    }
                }

                $this->collSuppliers = $collSuppliers;
                $this->collSuppliersPartial = false;
            }
        }

        return $this->collSuppliers;
    }

    /**
     * Sets a collection of Supplier objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $suppliers A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Kota The current object (for fluent API support)
     */
    public function setSuppliers(PropelCollection $suppliers, PropelPDO $con = null)
    {
        $suppliersToDelete = $this->getSuppliers(new Criteria(), $con)->diff($suppliers);


        $this->suppliersScheduledForDeletion = $suppliersToDelete;

        foreach ($suppliersToDelete as $supplierRemoved) {
            $supplierRemoved->setKota(null);
        }

        $this->collSuppliers = null;
        foreach ($suppliers as $supplier) {
            $this->addSupplier($supplier);
        }

        $this->collSuppliers = $suppliers;
        $this->collSuppliersPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Supplier objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Supplier objects.
     * @throws PropelException
     */
    public function countSuppliers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collSuppliersPartial && !$this->isNew();
        if (null === $this->collSuppliers || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collSuppliers) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getSuppliers());
            }
            $query = SupplierQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByKota($this)
                ->count($con);
        }

        return count($this->collSuppliers);
    }

    /**
     * Method called to associate a Supplier object to this object
     * through the Supplier foreign key attribute.
     *
     * @param    Supplier $l Supplier
     * @return Kota The current object (for fluent API support)
     */
    public function addSupplier(Supplier $l)
    {
        if ($this->collSuppliers === null) {
            $this->initSuppliers();
            $this->collSuppliersPartial = true;
        }

        if (!in_array($l, $this->collSuppliers->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddSupplier($l);

            if ($this->suppliersScheduledForDeletion and $this->suppliersScheduledForDeletion->contains($l)) {
                $this->suppliersScheduledForDeletion->remove($this->suppliersScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Supplier $supplier The supplier object to add.
     */
    protected function doAddSupplier($supplier)
    {
        $this->collSuppliers[]= $supplier;
        $supplier->setKota($this);
    }

    /**
     * @param	Supplier $supplier The supplier object to remove.
     * @return Kota The current object (for fluent API support)
     */
    public function removeSupplier($supplier)
    {
        if ($this->getSuppliers()->contains($supplier)) {
            $this->collSuppliers->remove($this->collSuppliers->search($supplier));
            if (null === $this->suppliersScheduledForDeletion) {
                $this->suppliersScheduledForDeletion = clone $this->collSuppliers;
                $this->suppliersScheduledForDeletion->clear();
            }
            $this->suppliersScheduledForDeletion[]= $supplier;
            $supplier->setKota(null);
        }

        return $this;
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->nama_kota = null;
        $this->id_propinsi = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collCustomerss) {
                foreach ($this->collCustomerss as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collProdusens) {
                foreach ($this->collProdusens as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collSuppliers) {
                foreach ($this->collSuppliers as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aPropinsi instanceof Persistent) {
              $this->aPropinsi->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collCustomerss instanceof PropelCollection) {
            $this->collCustomerss->clearIterator();
        }
        $this->collCustomerss = null;
        if ($this->collProdusens instanceof PropelCollection) {
            $this->collProdusens->clearIterator();
        }
        $this->collProdusens = null;
        if ($this->collSuppliers instanceof PropelCollection) {
            $this->collSuppliers->clearIterator();
        }
        $this->collSuppliers = null;
        $this->aPropinsi = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(KotaPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseKota:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
