<?php


/**
 * Base class that represents a row from the 'barang' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.7.0 on:
 *
 * 07/21/15 02:00:06
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseBarang extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'BarangPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        BarangPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the nama_barang field.
     * @var        string
     */
    protected $nama_barang;

    /**
     * The value for the id_kategori field.
     * @var        int
     */
    protected $id_kategori;

    /**
     * The value for the stock field.
     * @var        int
     */
    protected $stock;

    /**
     * The value for the id_kemasan field.
     * @var        int
     */
    protected $id_kemasan;

    /**
     * The value for the id_produsen field.
     * @var        int
     */
    protected $id_produsen;

    /**
     * The value for the description field.
     * @var        string
     */
    protected $description;

    /**
     * @var        Kategori
     */
    protected $aKategori;

    /**
     * @var        Kemasan
     */
    protected $aKemasan;

    /**
     * @var        Produsen
     */
    protected $aProdusen;

    /**
     * @var        PropelObjectCollection|DetailBarangMasuk[] Collection to store aggregation of DetailBarangMasuk objects.
     */
    protected $collDetailBarangMasuks;
    protected $collDetailBarangMasuksPartial;

    /**
     * @var        PropelObjectCollection|DetailTransaksi[] Collection to store aggregation of DetailTransaksi objects.
     */
    protected $collDetailTransaksis;
    protected $collDetailTransaksisPartial;

    /**
     * @var        PropelObjectCollection|Harga[] Collection to store aggregation of Harga objects.
     */
    protected $collHargas;
    protected $collHargasPartial;

    /**
     * @var        PropelObjectCollection|LinkBarangRak[] Collection to store aggregation of LinkBarangRak objects.
     */
    protected $collLinkBarangRaks;
    protected $collLinkBarangRaksPartial;

    /**
     * @var        PropelObjectCollection|LogBarang[] Collection to store aggregation of LogBarang objects.
     */
    protected $collLogBarangs;
    protected $collLogBarangsPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $detailBarangMasuksScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $detailTransaksisScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $hargasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $linkBarangRaksScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $logBarangsScheduledForDeletion = null;

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {

        return $this->id;
    }

    /**
     * Get the [nama_barang] column value.
     *
     * @return string
     */
    public function getNamaBarang()
    {

        return $this->nama_barang;
    }

    /**
     * Get the [id_kategori] column value.
     *
     * @return int
     */
    public function getIdKategori()
    {

        return $this->id_kategori;
    }

    /**
     * Get the [stock] column value.
     *
     * @return int
     */
    public function getStock()
    {

        return $this->stock;
    }

    /**
     * Get the [id_kemasan] column value.
     *
     * @return int
     */
    public function getIdKemasan()
    {

        return $this->id_kemasan;
    }

    /**
     * Get the [id_produsen] column value.
     *
     * @return int
     */
    public function getIdProdusen()
    {

        return $this->id_produsen;
    }

    /**
     * Get the [description] column value.
     *
     * @return string
     */
    public function getDescription()
    {

        return $this->description;
    }

    /**
     * Set the value of [id] column.
     *
     * @param  int $v new value
     * @return Barang The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = BarangPeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [nama_barang] column.
     *
     * @param  string $v new value
     * @return Barang The current object (for fluent API support)
     */
    public function setNamaBarang($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (string) $v;
        }

        if ($this->nama_barang !== $v) {
            $this->nama_barang = $v;
            $this->modifiedColumns[] = BarangPeer::NAMA_BARANG;
        }


        return $this;
    } // setNamaBarang()

    /**
     * Set the value of [id_kategori] column.
     *
     * @param  int $v new value
     * @return Barang The current object (for fluent API support)
     */
    public function setIdKategori($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->id_kategori !== $v) {
            $this->id_kategori = $v;
            $this->modifiedColumns[] = BarangPeer::ID_KATEGORI;
        }

        if ($this->aKategori !== null && $this->aKategori->getId() !== $v) {
            $this->aKategori = null;
        }


        return $this;
    } // setIdKategori()

    /**
     * Set the value of [stock] column.
     *
     * @param  int $v new value
     * @return Barang The current object (for fluent API support)
     */
    public function setStock($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->stock !== $v) {
            $this->stock = $v;
            $this->modifiedColumns[] = BarangPeer::STOCK;
        }


        return $this;
    } // setStock()

    /**
     * Set the value of [id_kemasan] column.
     *
     * @param  int $v new value
     * @return Barang The current object (for fluent API support)
     */
    public function setIdKemasan($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->id_kemasan !== $v) {
            $this->id_kemasan = $v;
            $this->modifiedColumns[] = BarangPeer::ID_KEMASAN;
        }

        if ($this->aKemasan !== null && $this->aKemasan->getId() !== $v) {
            $this->aKemasan = null;
        }


        return $this;
    } // setIdKemasan()

    /**
     * Set the value of [id_produsen] column.
     *
     * @param  int $v new value
     * @return Barang The current object (for fluent API support)
     */
    public function setIdProdusen($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->id_produsen !== $v) {
            $this->id_produsen = $v;
            $this->modifiedColumns[] = BarangPeer::ID_PRODUSEN;
        }

        if ($this->aProdusen !== null && $this->aProdusen->getId() !== $v) {
            $this->aProdusen = null;
        }


        return $this;
    } // setIdProdusen()

    /**
     * Set the value of [description] column.
     *
     * @param  string $v new value
     * @return Barang The current object (for fluent API support)
     */
    public function setDescription($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (string) $v;
        }

        if ($this->description !== $v) {
            $this->description = $v;
            $this->modifiedColumns[] = BarangPeer::DESCRIPTION;
        }


        return $this;
    } // setDescription()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->nama_barang = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->id_kategori = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->stock = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
            $this->id_kemasan = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
            $this->id_produsen = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
            $this->description = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 7; // 7 = BarangPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Barang object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aKategori !== null && $this->id_kategori !== $this->aKategori->getId()) {
            $this->aKategori = null;
        }
        if ($this->aKemasan !== null && $this->id_kemasan !== $this->aKemasan->getId()) {
            $this->aKemasan = null;
        }
        if ($this->aProdusen !== null && $this->id_produsen !== $this->aProdusen->getId()) {
            $this->aProdusen = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(BarangPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = BarangPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aKategori = null;
            $this->aKemasan = null;
            $this->aProdusen = null;
            $this->collDetailBarangMasuks = null;

            $this->collDetailTransaksis = null;

            $this->collHargas = null;

            $this->collLinkBarangRaks = null;

            $this->collLogBarangs = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(BarangPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = BarangQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseBarang:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseBarang:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(BarangPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseBarang:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseBarang:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                BarangPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aKategori !== null) {
                if ($this->aKategori->isModified() || $this->aKategori->isNew()) {
                    $affectedRows += $this->aKategori->save($con);
                }
                $this->setKategori($this->aKategori);
            }

            if ($this->aKemasan !== null) {
                if ($this->aKemasan->isModified() || $this->aKemasan->isNew()) {
                    $affectedRows += $this->aKemasan->save($con);
                }
                $this->setKemasan($this->aKemasan);
            }

            if ($this->aProdusen !== null) {
                if ($this->aProdusen->isModified() || $this->aProdusen->isNew()) {
                    $affectedRows += $this->aProdusen->save($con);
                }
                $this->setProdusen($this->aProdusen);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->detailBarangMasuksScheduledForDeletion !== null) {
                if (!$this->detailBarangMasuksScheduledForDeletion->isEmpty()) {
                    DetailBarangMasukQuery::create()
                        ->filterByPrimaryKeys($this->detailBarangMasuksScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->detailBarangMasuksScheduledForDeletion = null;
                }
            }

            if ($this->collDetailBarangMasuks !== null) {
                foreach ($this->collDetailBarangMasuks as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->detailTransaksisScheduledForDeletion !== null) {
                if (!$this->detailTransaksisScheduledForDeletion->isEmpty()) {
                    DetailTransaksiQuery::create()
                        ->filterByPrimaryKeys($this->detailTransaksisScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->detailTransaksisScheduledForDeletion = null;
                }
            }

            if ($this->collDetailTransaksis !== null) {
                foreach ($this->collDetailTransaksis as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->hargasScheduledForDeletion !== null) {
                if (!$this->hargasScheduledForDeletion->isEmpty()) {
                    HargaQuery::create()
                        ->filterByPrimaryKeys($this->hargasScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->hargasScheduledForDeletion = null;
                }
            }

            if ($this->collHargas !== null) {
                foreach ($this->collHargas as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->linkBarangRaksScheduledForDeletion !== null) {
                if (!$this->linkBarangRaksScheduledForDeletion->isEmpty()) {
                    LinkBarangRakQuery::create()
                        ->filterByPrimaryKeys($this->linkBarangRaksScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->linkBarangRaksScheduledForDeletion = null;
                }
            }

            if ($this->collLinkBarangRaks !== null) {
                foreach ($this->collLinkBarangRaks as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->logBarangsScheduledForDeletion !== null) {
                if (!$this->logBarangsScheduledForDeletion->isEmpty()) {
                    LogBarangQuery::create()
                        ->filterByPrimaryKeys($this->logBarangsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->logBarangsScheduledForDeletion = null;
                }
            }

            if ($this->collLogBarangs !== null) {
                foreach ($this->collLogBarangs as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = BarangPeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . BarangPeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(BarangPeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`id`';
        }
        if ($this->isColumnModified(BarangPeer::NAMA_BARANG)) {
            $modifiedColumns[':p' . $index++]  = '`nama_barang`';
        }
        if ($this->isColumnModified(BarangPeer::ID_KATEGORI)) {
            $modifiedColumns[':p' . $index++]  = '`id_kategori`';
        }
        if ($this->isColumnModified(BarangPeer::STOCK)) {
            $modifiedColumns[':p' . $index++]  = '`stock`';
        }
        if ($this->isColumnModified(BarangPeer::ID_KEMASAN)) {
            $modifiedColumns[':p' . $index++]  = '`id_kemasan`';
        }
        if ($this->isColumnModified(BarangPeer::ID_PRODUSEN)) {
            $modifiedColumns[':p' . $index++]  = '`id_produsen`';
        }
        if ($this->isColumnModified(BarangPeer::DESCRIPTION)) {
            $modifiedColumns[':p' . $index++]  = '`description`';
        }

        $sql = sprintf(
            'INSERT INTO `barang` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`id`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`nama_barang`':
                        $stmt->bindValue($identifier, $this->nama_barang, PDO::PARAM_STR);
                        break;
                    case '`id_kategori`':
                        $stmt->bindValue($identifier, $this->id_kategori, PDO::PARAM_INT);
                        break;
                    case '`stock`':
                        $stmt->bindValue($identifier, $this->stock, PDO::PARAM_INT);
                        break;
                    case '`id_kemasan`':
                        $stmt->bindValue($identifier, $this->id_kemasan, PDO::PARAM_INT);
                        break;
                    case '`id_produsen`':
                        $stmt->bindValue($identifier, $this->id_produsen, PDO::PARAM_INT);
                        break;
                    case '`description`':
                        $stmt->bindValue($identifier, $this->description, PDO::PARAM_STR);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aKategori !== null) {
                if (!$this->aKategori->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aKategori->getValidationFailures());
                }
            }

            if ($this->aKemasan !== null) {
                if (!$this->aKemasan->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aKemasan->getValidationFailures());
                }
            }

            if ($this->aProdusen !== null) {
                if (!$this->aProdusen->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aProdusen->getValidationFailures());
                }
            }


            if (($retval = BarangPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collDetailBarangMasuks !== null) {
                    foreach ($this->collDetailBarangMasuks as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collDetailTransaksis !== null) {
                    foreach ($this->collDetailTransaksis as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collHargas !== null) {
                    foreach ($this->collHargas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collLinkBarangRaks !== null) {
                    foreach ($this->collLinkBarangRaks as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collLogBarangs !== null) {
                    foreach ($this->collLogBarangs as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = BarangPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getNamaBarang();
                break;
            case 2:
                return $this->getIdKategori();
                break;
            case 3:
                return $this->getStock();
                break;
            case 4:
                return $this->getIdKemasan();
                break;
            case 5:
                return $this->getIdProdusen();
                break;
            case 6:
                return $this->getDescription();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Barang'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Barang'][$this->getPrimaryKey()] = true;
        $keys = BarangPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getNamaBarang(),
            $keys[2] => $this->getIdKategori(),
            $keys[3] => $this->getStock(),
            $keys[4] => $this->getIdKemasan(),
            $keys[5] => $this->getIdProdusen(),
            $keys[6] => $this->getDescription(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }

        if ($includeForeignObjects) {
            if (null !== $this->aKategori) {
                $result['Kategori'] = $this->aKategori->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aKemasan) {
                $result['Kemasan'] = $this->aKemasan->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aProdusen) {
                $result['Produsen'] = $this->aProdusen->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collDetailBarangMasuks) {
                $result['DetailBarangMasuks'] = $this->collDetailBarangMasuks->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collDetailTransaksis) {
                $result['DetailTransaksis'] = $this->collDetailTransaksis->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collHargas) {
                $result['Hargas'] = $this->collHargas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collLinkBarangRaks) {
                $result['LinkBarangRaks'] = $this->collLinkBarangRaks->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collLogBarangs) {
                $result['LogBarangs'] = $this->collLogBarangs->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = BarangPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setNamaBarang($value);
                break;
            case 2:
                $this->setIdKategori($value);
                break;
            case 3:
                $this->setStock($value);
                break;
            case 4:
                $this->setIdKemasan($value);
                break;
            case 5:
                $this->setIdProdusen($value);
                break;
            case 6:
                $this->setDescription($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = BarangPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setNamaBarang($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setIdKategori($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setStock($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setIdKemasan($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setIdProdusen($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setDescription($arr[$keys[6]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(BarangPeer::DATABASE_NAME);

        if ($this->isColumnModified(BarangPeer::ID)) $criteria->add(BarangPeer::ID, $this->id);
        if ($this->isColumnModified(BarangPeer::NAMA_BARANG)) $criteria->add(BarangPeer::NAMA_BARANG, $this->nama_barang);
        if ($this->isColumnModified(BarangPeer::ID_KATEGORI)) $criteria->add(BarangPeer::ID_KATEGORI, $this->id_kategori);
        if ($this->isColumnModified(BarangPeer::STOCK)) $criteria->add(BarangPeer::STOCK, $this->stock);
        if ($this->isColumnModified(BarangPeer::ID_KEMASAN)) $criteria->add(BarangPeer::ID_KEMASAN, $this->id_kemasan);
        if ($this->isColumnModified(BarangPeer::ID_PRODUSEN)) $criteria->add(BarangPeer::ID_PRODUSEN, $this->id_produsen);
        if ($this->isColumnModified(BarangPeer::DESCRIPTION)) $criteria->add(BarangPeer::DESCRIPTION, $this->description);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(BarangPeer::DATABASE_NAME);
        $criteria->add(BarangPeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Barang (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setNamaBarang($this->getNamaBarang());
        $copyObj->setIdKategori($this->getIdKategori());
        $copyObj->setStock($this->getStock());
        $copyObj->setIdKemasan($this->getIdKemasan());
        $copyObj->setIdProdusen($this->getIdProdusen());
        $copyObj->setDescription($this->getDescription());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getDetailBarangMasuks() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addDetailBarangMasuk($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getDetailTransaksis() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addDetailTransaksi($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getHargas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addHarga($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getLinkBarangRaks() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addLinkBarangRak($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getLogBarangs() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addLogBarang($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Barang Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return BarangPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new BarangPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Kategori object.
     *
     * @param                  Kategori $v
     * @return Barang The current object (for fluent API support)
     * @throws PropelException
     */
    public function setKategori(Kategori $v = null)
    {
        if ($v === null) {
            $this->setIdKategori(NULL);
        } else {
            $this->setIdKategori($v->getId());
        }

        $this->aKategori = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Kategori object, it will not be re-added.
        if ($v !== null) {
            $v->addBarang($this);
        }


        return $this;
    }


    /**
     * Get the associated Kategori object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Kategori The associated Kategori object.
     * @throws PropelException
     */
    public function getKategori(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aKategori === null && ($this->id_kategori !== null) && $doQuery) {
            $this->aKategori = KategoriQuery::create()->findPk($this->id_kategori, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aKategori->addBarangs($this);
             */
        }

        return $this->aKategori;
    }

    /**
     * Declares an association between this object and a Kemasan object.
     *
     * @param                  Kemasan $v
     * @return Barang The current object (for fluent API support)
     * @throws PropelException
     */
    public function setKemasan(Kemasan $v = null)
    {
        if ($v === null) {
            $this->setIdKemasan(NULL);
        } else {
            $this->setIdKemasan($v->getId());
        }

        $this->aKemasan = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Kemasan object, it will not be re-added.
        if ($v !== null) {
            $v->addBarang($this);
        }


        return $this;
    }


    /**
     * Get the associated Kemasan object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Kemasan The associated Kemasan object.
     * @throws PropelException
     */
    public function getKemasan(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aKemasan === null && ($this->id_kemasan !== null) && $doQuery) {
            $this->aKemasan = KemasanQuery::create()->findPk($this->id_kemasan, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aKemasan->addBarangs($this);
             */
        }

        return $this->aKemasan;
    }

    /**
     * Declares an association between this object and a Produsen object.
     *
     * @param                  Produsen $v
     * @return Barang The current object (for fluent API support)
     * @throws PropelException
     */
    public function setProdusen(Produsen $v = null)
    {
        if ($v === null) {
            $this->setIdProdusen(NULL);
        } else {
            $this->setIdProdusen($v->getId());
        }

        $this->aProdusen = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Produsen object, it will not be re-added.
        if ($v !== null) {
            $v->addBarang($this);
        }


        return $this;
    }


    /**
     * Get the associated Produsen object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Produsen The associated Produsen object.
     * @throws PropelException
     */
    public function getProdusen(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aProdusen === null && ($this->id_produsen !== null) && $doQuery) {
            $this->aProdusen = ProdusenQuery::create()->findPk($this->id_produsen, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aProdusen->addBarangs($this);
             */
        }

        return $this->aProdusen;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('DetailBarangMasuk' == $relationName) {
            $this->initDetailBarangMasuks();
        }
        if ('DetailTransaksi' == $relationName) {
            $this->initDetailTransaksis();
        }
        if ('Harga' == $relationName) {
            $this->initHargas();
        }
        if ('LinkBarangRak' == $relationName) {
            $this->initLinkBarangRaks();
        }
        if ('LogBarang' == $relationName) {
            $this->initLogBarangs();
        }
    }

    /**
     * Clears out the collDetailBarangMasuks collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Barang The current object (for fluent API support)
     * @see        addDetailBarangMasuks()
     */
    public function clearDetailBarangMasuks()
    {
        $this->collDetailBarangMasuks = null; // important to set this to null since that means it is uninitialized
        $this->collDetailBarangMasuksPartial = null;

        return $this;
    }

    /**
     * reset is the collDetailBarangMasuks collection loaded partially
     *
     * @return void
     */
    public function resetPartialDetailBarangMasuks($v = true)
    {
        $this->collDetailBarangMasuksPartial = $v;
    }

    /**
     * Initializes the collDetailBarangMasuks collection.
     *
     * By default this just sets the collDetailBarangMasuks collection to an empty array (like clearcollDetailBarangMasuks());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initDetailBarangMasuks($overrideExisting = true)
    {
        if (null !== $this->collDetailBarangMasuks && !$overrideExisting) {
            return;
        }
        $this->collDetailBarangMasuks = new PropelObjectCollection();
        $this->collDetailBarangMasuks->setModel('DetailBarangMasuk');
    }

    /**
     * Gets an array of DetailBarangMasuk objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Barang is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|DetailBarangMasuk[] List of DetailBarangMasuk objects
     * @throws PropelException
     */
    public function getDetailBarangMasuks($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collDetailBarangMasuksPartial && !$this->isNew();
        if (null === $this->collDetailBarangMasuks || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collDetailBarangMasuks) {
                // return empty collection
                $this->initDetailBarangMasuks();
            } else {
                $collDetailBarangMasuks = DetailBarangMasukQuery::create(null, $criteria)
                    ->filterByBarang($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collDetailBarangMasuksPartial && count($collDetailBarangMasuks)) {
                      $this->initDetailBarangMasuks(false);

                      foreach ($collDetailBarangMasuks as $obj) {
                        if (false == $this->collDetailBarangMasuks->contains($obj)) {
                          $this->collDetailBarangMasuks->append($obj);
                        }
                      }

                      $this->collDetailBarangMasuksPartial = true;
                    }

                    $collDetailBarangMasuks->getInternalIterator()->rewind();

                    return $collDetailBarangMasuks;
                }

                if ($partial && $this->collDetailBarangMasuks) {
                    foreach ($this->collDetailBarangMasuks as $obj) {
                        if ($obj->isNew()) {
                            $collDetailBarangMasuks[] = $obj;
                        }
                    }
                }

                $this->collDetailBarangMasuks = $collDetailBarangMasuks;
                $this->collDetailBarangMasuksPartial = false;
            }
        }

        return $this->collDetailBarangMasuks;
    }

    /**
     * Sets a collection of DetailBarangMasuk objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $detailBarangMasuks A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Barang The current object (for fluent API support)
     */
    public function setDetailBarangMasuks(PropelCollection $detailBarangMasuks, PropelPDO $con = null)
    {
        $detailBarangMasuksToDelete = $this->getDetailBarangMasuks(new Criteria(), $con)->diff($detailBarangMasuks);


        $this->detailBarangMasuksScheduledForDeletion = $detailBarangMasuksToDelete;

        foreach ($detailBarangMasuksToDelete as $detailBarangMasukRemoved) {
            $detailBarangMasukRemoved->setBarang(null);
        }

        $this->collDetailBarangMasuks = null;
        foreach ($detailBarangMasuks as $detailBarangMasuk) {
            $this->addDetailBarangMasuk($detailBarangMasuk);
        }

        $this->collDetailBarangMasuks = $detailBarangMasuks;
        $this->collDetailBarangMasuksPartial = false;

        return $this;
    }

    /**
     * Returns the number of related DetailBarangMasuk objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related DetailBarangMasuk objects.
     * @throws PropelException
     */
    public function countDetailBarangMasuks(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collDetailBarangMasuksPartial && !$this->isNew();
        if (null === $this->collDetailBarangMasuks || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collDetailBarangMasuks) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getDetailBarangMasuks());
            }
            $query = DetailBarangMasukQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByBarang($this)
                ->count($con);
        }

        return count($this->collDetailBarangMasuks);
    }

    /**
     * Method called to associate a DetailBarangMasuk object to this object
     * through the DetailBarangMasuk foreign key attribute.
     *
     * @param    DetailBarangMasuk $l DetailBarangMasuk
     * @return Barang The current object (for fluent API support)
     */
    public function addDetailBarangMasuk(DetailBarangMasuk $l)
    {
        if ($this->collDetailBarangMasuks === null) {
            $this->initDetailBarangMasuks();
            $this->collDetailBarangMasuksPartial = true;
        }

        if (!in_array($l, $this->collDetailBarangMasuks->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddDetailBarangMasuk($l);

            if ($this->detailBarangMasuksScheduledForDeletion and $this->detailBarangMasuksScheduledForDeletion->contains($l)) {
                $this->detailBarangMasuksScheduledForDeletion->remove($this->detailBarangMasuksScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	DetailBarangMasuk $detailBarangMasuk The detailBarangMasuk object to add.
     */
    protected function doAddDetailBarangMasuk($detailBarangMasuk)
    {
        $this->collDetailBarangMasuks[]= $detailBarangMasuk;
        $detailBarangMasuk->setBarang($this);
    }

    /**
     * @param	DetailBarangMasuk $detailBarangMasuk The detailBarangMasuk object to remove.
     * @return Barang The current object (for fluent API support)
     */
    public function removeDetailBarangMasuk($detailBarangMasuk)
    {
        if ($this->getDetailBarangMasuks()->contains($detailBarangMasuk)) {
            $this->collDetailBarangMasuks->remove($this->collDetailBarangMasuks->search($detailBarangMasuk));
            if (null === $this->detailBarangMasuksScheduledForDeletion) {
                $this->detailBarangMasuksScheduledForDeletion = clone $this->collDetailBarangMasuks;
                $this->detailBarangMasuksScheduledForDeletion->clear();
            }
            $this->detailBarangMasuksScheduledForDeletion[]= $detailBarangMasuk;
            $detailBarangMasuk->setBarang(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Barang is new, it will return
     * an empty collection; or if this Barang has previously
     * been saved, it will retrieve related DetailBarangMasuks from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Barang.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|DetailBarangMasuk[] List of DetailBarangMasuk objects
     */
    public function getDetailBarangMasuksJoinBarangMasuk($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DetailBarangMasukQuery::create(null, $criteria);
        $query->joinWith('BarangMasuk', $join_behavior);

        return $this->getDetailBarangMasuks($query, $con);
    }

    /**
     * Clears out the collDetailTransaksis collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Barang The current object (for fluent API support)
     * @see        addDetailTransaksis()
     */
    public function clearDetailTransaksis()
    {
        $this->collDetailTransaksis = null; // important to set this to null since that means it is uninitialized
        $this->collDetailTransaksisPartial = null;

        return $this;
    }

    /**
     * reset is the collDetailTransaksis collection loaded partially
     *
     * @return void
     */
    public function resetPartialDetailTransaksis($v = true)
    {
        $this->collDetailTransaksisPartial = $v;
    }

    /**
     * Initializes the collDetailTransaksis collection.
     *
     * By default this just sets the collDetailTransaksis collection to an empty array (like clearcollDetailTransaksis());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initDetailTransaksis($overrideExisting = true)
    {
        if (null !== $this->collDetailTransaksis && !$overrideExisting) {
            return;
        }
        $this->collDetailTransaksis = new PropelObjectCollection();
        $this->collDetailTransaksis->setModel('DetailTransaksi');
    }

    /**
     * Gets an array of DetailTransaksi objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Barang is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|DetailTransaksi[] List of DetailTransaksi objects
     * @throws PropelException
     */
    public function getDetailTransaksis($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collDetailTransaksisPartial && !$this->isNew();
        if (null === $this->collDetailTransaksis || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collDetailTransaksis) {
                // return empty collection
                $this->initDetailTransaksis();
            } else {
                $collDetailTransaksis = DetailTransaksiQuery::create(null, $criteria)
                    ->filterByBarang($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collDetailTransaksisPartial && count($collDetailTransaksis)) {
                      $this->initDetailTransaksis(false);

                      foreach ($collDetailTransaksis as $obj) {
                        if (false == $this->collDetailTransaksis->contains($obj)) {
                          $this->collDetailTransaksis->append($obj);
                        }
                      }

                      $this->collDetailTransaksisPartial = true;
                    }

                    $collDetailTransaksis->getInternalIterator()->rewind();

                    return $collDetailTransaksis;
                }

                if ($partial && $this->collDetailTransaksis) {
                    foreach ($this->collDetailTransaksis as $obj) {
                        if ($obj->isNew()) {
                            $collDetailTransaksis[] = $obj;
                        }
                    }
                }

                $this->collDetailTransaksis = $collDetailTransaksis;
                $this->collDetailTransaksisPartial = false;
            }
        }

        return $this->collDetailTransaksis;
    }

    /**
     * Sets a collection of DetailTransaksi objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $detailTransaksis A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Barang The current object (for fluent API support)
     */
    public function setDetailTransaksis(PropelCollection $detailTransaksis, PropelPDO $con = null)
    {
        $detailTransaksisToDelete = $this->getDetailTransaksis(new Criteria(), $con)->diff($detailTransaksis);


        $this->detailTransaksisScheduledForDeletion = $detailTransaksisToDelete;

        foreach ($detailTransaksisToDelete as $detailTransaksiRemoved) {
            $detailTransaksiRemoved->setBarang(null);
        }

        $this->collDetailTransaksis = null;
        foreach ($detailTransaksis as $detailTransaksi) {
            $this->addDetailTransaksi($detailTransaksi);
        }

        $this->collDetailTransaksis = $detailTransaksis;
        $this->collDetailTransaksisPartial = false;

        return $this;
    }

    /**
     * Returns the number of related DetailTransaksi objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related DetailTransaksi objects.
     * @throws PropelException
     */
    public function countDetailTransaksis(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collDetailTransaksisPartial && !$this->isNew();
        if (null === $this->collDetailTransaksis || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collDetailTransaksis) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getDetailTransaksis());
            }
            $query = DetailTransaksiQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByBarang($this)
                ->count($con);
        }

        return count($this->collDetailTransaksis);
    }

    /**
     * Method called to associate a DetailTransaksi object to this object
     * through the DetailTransaksi foreign key attribute.
     *
     * @param    DetailTransaksi $l DetailTransaksi
     * @return Barang The current object (for fluent API support)
     */
    public function addDetailTransaksi(DetailTransaksi $l)
    {
        if ($this->collDetailTransaksis === null) {
            $this->initDetailTransaksis();
            $this->collDetailTransaksisPartial = true;
        }

        if (!in_array($l, $this->collDetailTransaksis->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddDetailTransaksi($l);

            if ($this->detailTransaksisScheduledForDeletion and $this->detailTransaksisScheduledForDeletion->contains($l)) {
                $this->detailTransaksisScheduledForDeletion->remove($this->detailTransaksisScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	DetailTransaksi $detailTransaksi The detailTransaksi object to add.
     */
    protected function doAddDetailTransaksi($detailTransaksi)
    {
        $this->collDetailTransaksis[]= $detailTransaksi;
        $detailTransaksi->setBarang($this);
    }

    /**
     * @param	DetailTransaksi $detailTransaksi The detailTransaksi object to remove.
     * @return Barang The current object (for fluent API support)
     */
    public function removeDetailTransaksi($detailTransaksi)
    {
        if ($this->getDetailTransaksis()->contains($detailTransaksi)) {
            $this->collDetailTransaksis->remove($this->collDetailTransaksis->search($detailTransaksi));
            if (null === $this->detailTransaksisScheduledForDeletion) {
                $this->detailTransaksisScheduledForDeletion = clone $this->collDetailTransaksis;
                $this->detailTransaksisScheduledForDeletion->clear();
            }
            $this->detailTransaksisScheduledForDeletion[]= $detailTransaksi;
            $detailTransaksi->setBarang(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Barang is new, it will return
     * an empty collection; or if this Barang has previously
     * been saved, it will retrieve related DetailTransaksis from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Barang.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|DetailTransaksi[] List of DetailTransaksi objects
     */
    public function getDetailTransaksisJoinTransaksi($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DetailTransaksiQuery::create(null, $criteria);
        $query->joinWith('Transaksi', $join_behavior);

        return $this->getDetailTransaksis($query, $con);
    }

    /**
     * Clears out the collHargas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Barang The current object (for fluent API support)
     * @see        addHargas()
     */
    public function clearHargas()
    {
        $this->collHargas = null; // important to set this to null since that means it is uninitialized
        $this->collHargasPartial = null;

        return $this;
    }

    /**
     * reset is the collHargas collection loaded partially
     *
     * @return void
     */
    public function resetPartialHargas($v = true)
    {
        $this->collHargasPartial = $v;
    }

    /**
     * Initializes the collHargas collection.
     *
     * By default this just sets the collHargas collection to an empty array (like clearcollHargas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initHargas($overrideExisting = true)
    {
        if (null !== $this->collHargas && !$overrideExisting) {
            return;
        }
        $this->collHargas = new PropelObjectCollection();
        $this->collHargas->setModel('Harga');
    }

    /**
     * Gets an array of Harga objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Barang is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Harga[] List of Harga objects
     * @throws PropelException
     */
    public function getHargas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collHargasPartial && !$this->isNew();
        if (null === $this->collHargas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collHargas) {
                // return empty collection
                $this->initHargas();
            } else {
                $collHargas = HargaQuery::create(null, $criteria)
                    ->filterByBarang($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collHargasPartial && count($collHargas)) {
                      $this->initHargas(false);

                      foreach ($collHargas as $obj) {
                        if (false == $this->collHargas->contains($obj)) {
                          $this->collHargas->append($obj);
                        }
                      }

                      $this->collHargasPartial = true;
                    }

                    $collHargas->getInternalIterator()->rewind();

                    return $collHargas;
                }

                if ($partial && $this->collHargas) {
                    foreach ($this->collHargas as $obj) {
                        if ($obj->isNew()) {
                            $collHargas[] = $obj;
                        }
                    }
                }

                $this->collHargas = $collHargas;
                $this->collHargasPartial = false;
            }
        }

        return $this->collHargas;
    }

    /**
     * Sets a collection of Harga objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $hargas A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Barang The current object (for fluent API support)
     */
    public function setHargas(PropelCollection $hargas, PropelPDO $con = null)
    {
        $hargasToDelete = $this->getHargas(new Criteria(), $con)->diff($hargas);


        $this->hargasScheduledForDeletion = $hargasToDelete;

        foreach ($hargasToDelete as $hargaRemoved) {
            $hargaRemoved->setBarang(null);
        }

        $this->collHargas = null;
        foreach ($hargas as $harga) {
            $this->addHarga($harga);
        }

        $this->collHargas = $hargas;
        $this->collHargasPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Harga objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Harga objects.
     * @throws PropelException
     */
    public function countHargas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collHargasPartial && !$this->isNew();
        if (null === $this->collHargas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collHargas) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getHargas());
            }
            $query = HargaQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByBarang($this)
                ->count($con);
        }

        return count($this->collHargas);
    }

    /**
     * Method called to associate a Harga object to this object
     * through the Harga foreign key attribute.
     *
     * @param    Harga $l Harga
     * @return Barang The current object (for fluent API support)
     */
    public function addHarga(Harga $l)
    {
        if ($this->collHargas === null) {
            $this->initHargas();
            $this->collHargasPartial = true;
        }

        if (!in_array($l, $this->collHargas->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddHarga($l);

            if ($this->hargasScheduledForDeletion and $this->hargasScheduledForDeletion->contains($l)) {
                $this->hargasScheduledForDeletion->remove($this->hargasScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Harga $harga The harga object to add.
     */
    protected function doAddHarga($harga)
    {
        $this->collHargas[]= $harga;
        $harga->setBarang($this);
    }

    /**
     * @param	Harga $harga The harga object to remove.
     * @return Barang The current object (for fluent API support)
     */
    public function removeHarga($harga)
    {
        if ($this->getHargas()->contains($harga)) {
            $this->collHargas->remove($this->collHargas->search($harga));
            if (null === $this->hargasScheduledForDeletion) {
                $this->hargasScheduledForDeletion = clone $this->collHargas;
                $this->hargasScheduledForDeletion->clear();
            }
            $this->hargasScheduledForDeletion[]= $harga;
            $harga->setBarang(null);
        }

        return $this;
    }

    /**
     * Clears out the collLinkBarangRaks collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Barang The current object (for fluent API support)
     * @see        addLinkBarangRaks()
     */
    public function clearLinkBarangRaks()
    {
        $this->collLinkBarangRaks = null; // important to set this to null since that means it is uninitialized
        $this->collLinkBarangRaksPartial = null;

        return $this;
    }

    /**
     * reset is the collLinkBarangRaks collection loaded partially
     *
     * @return void
     */
    public function resetPartialLinkBarangRaks($v = true)
    {
        $this->collLinkBarangRaksPartial = $v;
    }

    /**
     * Initializes the collLinkBarangRaks collection.
     *
     * By default this just sets the collLinkBarangRaks collection to an empty array (like clearcollLinkBarangRaks());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initLinkBarangRaks($overrideExisting = true)
    {
        if (null !== $this->collLinkBarangRaks && !$overrideExisting) {
            return;
        }
        $this->collLinkBarangRaks = new PropelObjectCollection();
        $this->collLinkBarangRaks->setModel('LinkBarangRak');
    }

    /**
     * Gets an array of LinkBarangRak objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Barang is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|LinkBarangRak[] List of LinkBarangRak objects
     * @throws PropelException
     */
    public function getLinkBarangRaks($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collLinkBarangRaksPartial && !$this->isNew();
        if (null === $this->collLinkBarangRaks || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collLinkBarangRaks) {
                // return empty collection
                $this->initLinkBarangRaks();
            } else {
                $collLinkBarangRaks = LinkBarangRakQuery::create(null, $criteria)
                    ->filterByBarang($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collLinkBarangRaksPartial && count($collLinkBarangRaks)) {
                      $this->initLinkBarangRaks(false);

                      foreach ($collLinkBarangRaks as $obj) {
                        if (false == $this->collLinkBarangRaks->contains($obj)) {
                          $this->collLinkBarangRaks->append($obj);
                        }
                      }

                      $this->collLinkBarangRaksPartial = true;
                    }

                    $collLinkBarangRaks->getInternalIterator()->rewind();

                    return $collLinkBarangRaks;
                }

                if ($partial && $this->collLinkBarangRaks) {
                    foreach ($this->collLinkBarangRaks as $obj) {
                        if ($obj->isNew()) {
                            $collLinkBarangRaks[] = $obj;
                        }
                    }
                }

                $this->collLinkBarangRaks = $collLinkBarangRaks;
                $this->collLinkBarangRaksPartial = false;
            }
        }

        return $this->collLinkBarangRaks;
    }

    /**
     * Sets a collection of LinkBarangRak objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $linkBarangRaks A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Barang The current object (for fluent API support)
     */
    public function setLinkBarangRaks(PropelCollection $linkBarangRaks, PropelPDO $con = null)
    {
        $linkBarangRaksToDelete = $this->getLinkBarangRaks(new Criteria(), $con)->diff($linkBarangRaks);


        $this->linkBarangRaksScheduledForDeletion = $linkBarangRaksToDelete;

        foreach ($linkBarangRaksToDelete as $linkBarangRakRemoved) {
            $linkBarangRakRemoved->setBarang(null);
        }

        $this->collLinkBarangRaks = null;
        foreach ($linkBarangRaks as $linkBarangRak) {
            $this->addLinkBarangRak($linkBarangRak);
        }

        $this->collLinkBarangRaks = $linkBarangRaks;
        $this->collLinkBarangRaksPartial = false;

        return $this;
    }

    /**
     * Returns the number of related LinkBarangRak objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related LinkBarangRak objects.
     * @throws PropelException
     */
    public function countLinkBarangRaks(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collLinkBarangRaksPartial && !$this->isNew();
        if (null === $this->collLinkBarangRaks || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collLinkBarangRaks) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getLinkBarangRaks());
            }
            $query = LinkBarangRakQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByBarang($this)
                ->count($con);
        }

        return count($this->collLinkBarangRaks);
    }

    /**
     * Method called to associate a LinkBarangRak object to this object
     * through the LinkBarangRak foreign key attribute.
     *
     * @param    LinkBarangRak $l LinkBarangRak
     * @return Barang The current object (for fluent API support)
     */
    public function addLinkBarangRak(LinkBarangRak $l)
    {
        if ($this->collLinkBarangRaks === null) {
            $this->initLinkBarangRaks();
            $this->collLinkBarangRaksPartial = true;
        }

        if (!in_array($l, $this->collLinkBarangRaks->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddLinkBarangRak($l);

            if ($this->linkBarangRaksScheduledForDeletion and $this->linkBarangRaksScheduledForDeletion->contains($l)) {
                $this->linkBarangRaksScheduledForDeletion->remove($this->linkBarangRaksScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	LinkBarangRak $linkBarangRak The linkBarangRak object to add.
     */
    protected function doAddLinkBarangRak($linkBarangRak)
    {
        $this->collLinkBarangRaks[]= $linkBarangRak;
        $linkBarangRak->setBarang($this);
    }

    /**
     * @param	LinkBarangRak $linkBarangRak The linkBarangRak object to remove.
     * @return Barang The current object (for fluent API support)
     */
    public function removeLinkBarangRak($linkBarangRak)
    {
        if ($this->getLinkBarangRaks()->contains($linkBarangRak)) {
            $this->collLinkBarangRaks->remove($this->collLinkBarangRaks->search($linkBarangRak));
            if (null === $this->linkBarangRaksScheduledForDeletion) {
                $this->linkBarangRaksScheduledForDeletion = clone $this->collLinkBarangRaks;
                $this->linkBarangRaksScheduledForDeletion->clear();
            }
            $this->linkBarangRaksScheduledForDeletion[]= $linkBarangRak;
            $linkBarangRak->setBarang(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Barang is new, it will return
     * an empty collection; or if this Barang has previously
     * been saved, it will retrieve related LinkBarangRaks from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Barang.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|LinkBarangRak[] List of LinkBarangRak objects
     */
    public function getLinkBarangRaksJoinRak($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = LinkBarangRakQuery::create(null, $criteria);
        $query->joinWith('Rak', $join_behavior);

        return $this->getLinkBarangRaks($query, $con);
    }

    /**
     * Clears out the collLogBarangs collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Barang The current object (for fluent API support)
     * @see        addLogBarangs()
     */
    public function clearLogBarangs()
    {
        $this->collLogBarangs = null; // important to set this to null since that means it is uninitialized
        $this->collLogBarangsPartial = null;

        return $this;
    }

    /**
     * reset is the collLogBarangs collection loaded partially
     *
     * @return void
     */
    public function resetPartialLogBarangs($v = true)
    {
        $this->collLogBarangsPartial = $v;
    }

    /**
     * Initializes the collLogBarangs collection.
     *
     * By default this just sets the collLogBarangs collection to an empty array (like clearcollLogBarangs());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initLogBarangs($overrideExisting = true)
    {
        if (null !== $this->collLogBarangs && !$overrideExisting) {
            return;
        }
        $this->collLogBarangs = new PropelObjectCollection();
        $this->collLogBarangs->setModel('LogBarang');
    }

    /**
     * Gets an array of LogBarang objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Barang is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|LogBarang[] List of LogBarang objects
     * @throws PropelException
     */
    public function getLogBarangs($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collLogBarangsPartial && !$this->isNew();
        if (null === $this->collLogBarangs || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collLogBarangs) {
                // return empty collection
                $this->initLogBarangs();
            } else {
                $collLogBarangs = LogBarangQuery::create(null, $criteria)
                    ->filterByBarang($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collLogBarangsPartial && count($collLogBarangs)) {
                      $this->initLogBarangs(false);

                      foreach ($collLogBarangs as $obj) {
                        if (false == $this->collLogBarangs->contains($obj)) {
                          $this->collLogBarangs->append($obj);
                        }
                      }

                      $this->collLogBarangsPartial = true;
                    }

                    $collLogBarangs->getInternalIterator()->rewind();

                    return $collLogBarangs;
                }

                if ($partial && $this->collLogBarangs) {
                    foreach ($this->collLogBarangs as $obj) {
                        if ($obj->isNew()) {
                            $collLogBarangs[] = $obj;
                        }
                    }
                }

                $this->collLogBarangs = $collLogBarangs;
                $this->collLogBarangsPartial = false;
            }
        }

        return $this->collLogBarangs;
    }

    /**
     * Sets a collection of LogBarang objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $logBarangs A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Barang The current object (for fluent API support)
     */
    public function setLogBarangs(PropelCollection $logBarangs, PropelPDO $con = null)
    {
        $logBarangsToDelete = $this->getLogBarangs(new Criteria(), $con)->diff($logBarangs);


        $this->logBarangsScheduledForDeletion = $logBarangsToDelete;

        foreach ($logBarangsToDelete as $logBarangRemoved) {
            $logBarangRemoved->setBarang(null);
        }

        $this->collLogBarangs = null;
        foreach ($logBarangs as $logBarang) {
            $this->addLogBarang($logBarang);
        }

        $this->collLogBarangs = $logBarangs;
        $this->collLogBarangsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related LogBarang objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related LogBarang objects.
     * @throws PropelException
     */
    public function countLogBarangs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collLogBarangsPartial && !$this->isNew();
        if (null === $this->collLogBarangs || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collLogBarangs) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getLogBarangs());
            }
            $query = LogBarangQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByBarang($this)
                ->count($con);
        }

        return count($this->collLogBarangs);
    }

    /**
     * Method called to associate a LogBarang object to this object
     * through the LogBarang foreign key attribute.
     *
     * @param    LogBarang $l LogBarang
     * @return Barang The current object (for fluent API support)
     */
    public function addLogBarang(LogBarang $l)
    {
        if ($this->collLogBarangs === null) {
            $this->initLogBarangs();
            $this->collLogBarangsPartial = true;
        }

        if (!in_array($l, $this->collLogBarangs->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddLogBarang($l);

            if ($this->logBarangsScheduledForDeletion and $this->logBarangsScheduledForDeletion->contains($l)) {
                $this->logBarangsScheduledForDeletion->remove($this->logBarangsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	LogBarang $logBarang The logBarang object to add.
     */
    protected function doAddLogBarang($logBarang)
    {
        $this->collLogBarangs[]= $logBarang;
        $logBarang->setBarang($this);
    }

    /**
     * @param	LogBarang $logBarang The logBarang object to remove.
     * @return Barang The current object (for fluent API support)
     */
    public function removeLogBarang($logBarang)
    {
        if ($this->getLogBarangs()->contains($logBarang)) {
            $this->collLogBarangs->remove($this->collLogBarangs->search($logBarang));
            if (null === $this->logBarangsScheduledForDeletion) {
                $this->logBarangsScheduledForDeletion = clone $this->collLogBarangs;
                $this->logBarangsScheduledForDeletion->clear();
            }
            $this->logBarangsScheduledForDeletion[]= $logBarang;
            $logBarang->setBarang(null);
        }

        return $this;
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->nama_barang = null;
        $this->id_kategori = null;
        $this->stock = null;
        $this->id_kemasan = null;
        $this->id_produsen = null;
        $this->description = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collDetailBarangMasuks) {
                foreach ($this->collDetailBarangMasuks as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collDetailTransaksis) {
                foreach ($this->collDetailTransaksis as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collHargas) {
                foreach ($this->collHargas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collLinkBarangRaks) {
                foreach ($this->collLinkBarangRaks as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collLogBarangs) {
                foreach ($this->collLogBarangs as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aKategori instanceof Persistent) {
              $this->aKategori->clearAllReferences($deep);
            }
            if ($this->aKemasan instanceof Persistent) {
              $this->aKemasan->clearAllReferences($deep);
            }
            if ($this->aProdusen instanceof Persistent) {
              $this->aProdusen->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collDetailBarangMasuks instanceof PropelCollection) {
            $this->collDetailBarangMasuks->clearIterator();
        }
        $this->collDetailBarangMasuks = null;
        if ($this->collDetailTransaksis instanceof PropelCollection) {
            $this->collDetailTransaksis->clearIterator();
        }
        $this->collDetailTransaksis = null;
        if ($this->collHargas instanceof PropelCollection) {
            $this->collHargas->clearIterator();
        }
        $this->collHargas = null;
        if ($this->collLinkBarangRaks instanceof PropelCollection) {
            $this->collLinkBarangRaks->clearIterator();
        }
        $this->collLinkBarangRaks = null;
        if ($this->collLogBarangs instanceof PropelCollection) {
            $this->collLogBarangs->clearIterator();
        }
        $this->collLogBarangs = null;
        $this->aKategori = null;
        $this->aKemasan = null;
        $this->aProdusen = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(BarangPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseBarang:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
